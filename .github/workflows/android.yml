name: Android CI
on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Unit tests
        run: gradle testDebugUnitTest --stacktrace --console=plain --warning-mode=all > build.log 2>&1
      - name: Print compiler diagnostics
        if: always()
        run: |
          head -120 build.log
      - name: Upload compiler log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compiler-log
          path: build.log
      - name: Build signed prototype APKs
        run: gradle assembleDebug assembleRelease --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: trinetra-apks
          path: app/build/outputs/apk/**/*.apk
          if-no-files-found: error
